总体：
- 一个可以类似于微软“便签”的桌面应用，后端用python来写，前端用pythonqt来写。
- 最基础的功能：始终位于桌面，可以在拖动中自动检测桌面边长吸附，可以以长条的方式放在桌面一侧，可以调节UI透明度。
- 加载大模型api，辅助构建一些有趣的功能。

任务规划
- 任务完成标记 “complete”
- 历史任务记录
- 时间标记
- 历史任务记录+时间戳 注入大模型知识库

ai任务执行框架
- 关于工具本身的功能描述，也要注入大模型的知识库
- “聊聊”中的大模型应该也要具有一定的更改任务清单等延伸性的能力
- “聊聊”中的大模型应该要有上下文记忆
- 把所有的大模型调用都放入后台线程中，避免阻塞主线程
- 目前ai任务执行的框架还有很大的优化空间

文件管理
- 桌面文件获取功能
- 大模型调用时，要能够获取到桌面文件的内容
- 补充一个复制文件的技能

网页浏览/爬取信息
- 定时定期浏览网页，获取信息
- 可以设置浏览的时间间隔，也可以设置浏览的网页列表

桌宠
1.现在桌宠的询问存在这个问题，问了，查了，但是ai不告诉我，排查一下是agent本身的问题还是只是技能的问题。
2.在这个脚本中编写桌宠在屏幕上实现上下左右移动的真实位移逻辑。这里要注意，目前所有的动画素材默认都是桌宠面向左边，左右位移时要保证位移方向与桌宠面对方向一致，要注意妥善写在逻辑里控制。
3.将ai加载的关于命令桌宠的技能里，扩展到桌宠全部的动画模组，现在只有飞行和睡觉，其实还有一个走路动画的模组，要结合2来使用，当桌宠在非飞行动画状态下，发生了左右位移，就调用走路动画。
4.桌宠的行动范围要严格控制在屏幕上下左右的界限内，不能出屏幕导致无法控制。
5.最好能实现这样一个逻辑：向ai下达命令，让桌宠前往桌面某个图标的位置或某个具体坐标，ai能够自己构建指令前往那个区域。
6.设置一个重力系统，即当桌宠不在飞行状态时，会自行掉落到win系统桌面下方状态栏的上缘（即虚拟地面，作为y = 0的坐标基准线），只有桌宠进入飞行状态并位移，才会实现自身坐标y轴上的变化。
